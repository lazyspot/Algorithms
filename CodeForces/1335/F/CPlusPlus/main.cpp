#include <iostream>
#include <vector>
#include <string>

using namespace std;

vector<vector <int>> visited;
vector<string> board;

bool solve(int x, int y) {

}

int main()
{
    int n, m;
    cin>>n>>m;
    board.resize(n);
    for(int i=0; i<n; ++i) {
        cin>>board[i];
    }
    for(int i=0; i<board.size(); ++i) {
        for(int j=0; j<)
    }
    return 0;
}
